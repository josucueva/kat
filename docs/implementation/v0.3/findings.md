Finding 1:
Impact-centric retrieval can reduce semantic query overhead substantially.

Finding 2:
Semantic entry-point usefulness depends on graph reachability/completeness.

Finding 3:
A semantically valid but disconnected element can be poor retrieval context.

Finding 4:
Validation-to-physical-test resolution may be incomplete.

Finding 5:
Context aggregation is promising as automation over existing primitives, not necessarily as a new semantic capability.